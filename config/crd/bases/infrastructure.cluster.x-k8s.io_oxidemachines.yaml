---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: oxidemachines.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: OxideMachine
    listKind: OxideMachineList
    plural: oxidemachines
    singular: oxidemachine
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.providerID
      name: ProviderID
      type: string
    - jsonPath: .status.initialization.provisioned
      name: Ready
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OxideMachine is the Schema for the oxidemachines API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of OxideMachine
            properties:
              antiAffinityGroups:
                description: |-
                  AntiAffinityGroups is the list of anti-affinity group names or IDs that the Oxide instance
                  should use. Anti-affinity groups should be created outside the operator, else instance
                  creation will fail.
                  https://docs.oxide.computer/guides/deploying-workloads#_affinity_and_anti_affinity
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              diskSize:
                anyOf:
                - type: integer
                - type: string
                description: DiskSize is the size of the boot disk to allocate for
                  the instance, in bytes.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              imageID:
                description: ImageID is the ID of the boot disk image.
                type: string
              memory:
                anyOf:
                - type: integer
                - type: string
                description: Memory is the amount of memory to allocate for the instance,
                  in bytes.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              nCPUs:
                description: NCpus is the number of vCPUs to allocate for the instance.
                type: integer
              providerID:
                description: |-
                  ProviderID is a unique identifier for the Oxide instance for use in correlating across
                  kubernetes resources. Must match provider IDs generated by the Oxide cloud controller
                  manager.
                  https://cluster-api.sigs.k8s.io/reference/glossary#providerid
                type: string
              sshPublicKeys:
                description: |-
                  SSHPublicKeys is the list of SSH key names or IDs to attach to the Oxide instance. SSH keys
                  should be created outside the operator, else instance creation will fail.
                  https://docs.oxide.computer/guides/user-settings#manage-ssh-keys
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
            required:
            - diskSize
            - imageID
            - memory
            - nCPUs
            type: object
          status:
            description: status defines the observed state of OxideMachine
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the OxideMachine resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the
                  resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              initialization:
                properties:
                  provisioned:
                    type: boolean
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
