---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: oxidemachinetemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: OxideMachineTemplate
    listKind: OxideMachineTemplateList
    plural: oxidemachinetemplates
    singular: oxidemachinetemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OxideMachineTemplate is the Schema for the oxidemachinetemplates
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of OxideMachineTemplate
            properties:
              template:
                properties:
                  metadata:
                    description: |-
                      ObjectMeta is metadata that all persisted resources must have, which includes all objects
                      users must create. This is a copy of customizable fields from metav1.ObjectMeta.

                      ObjectMeta is embedded in `Machine.Spec`, `MachineDeployment.Template` and `MachineSet.Template`,
                      which are not top-level Kubernetes objects. Given that metav1.ObjectMeta has lots of special cases
                      and read-only fields which end up in the generated CRD validation, having it as a subset simplifies
                      the API and some issues that can impact user experience.

                      During the [upgrade to controller-tools@v2](https://github.com/kubernetes-sigs/cluster-api/pull/1054)
                      for v1alpha2, we noticed a failure would occur running Cluster API test suite against the new CRDs,
                      specifically `spec.metadata.creationTimestamp in body must be of type string: "null"`.
                      The investigation showed that `controller-tools@v2` behaves differently than its previous version
                      when handling types from [metav1](k8s.io/apimachinery/pkg/apis/meta/v1) package.

                      In more details, we found that embedded (non-top level) types that embedded `metav1.ObjectMeta`
                      had validation properties, including for `creationTimestamp` (metav1.Time).
                      The `metav1.Time` type specifies a custom json marshaller that, when IsZero() is true, returns `null`
                      which breaks validation because the field isn't marked as nullable.

                      In future versions, controller-tools@v2 might allow overriding the type and validation for embedded
                      types. When that happens, this hack should be revisited.
                    minProperties: 1
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          annotations is an unstructured key value map stored with a resource that may be
                          set by external tools to store and retrieve arbitrary metadata. They are not
                          queryable and should be preserved when modifying objects.
                          More info: http://kubernetes.io/docs/user-guide/annotations
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          labels is a map of string keys and values that can be used to organize and categorize
                          (scope and select) objects. May match selectors of replication controllers
                          and services.
                          More info: http://kubernetes.io/docs/user-guide/labels
                        type: object
                    type: object
                  spec:
                    description: OxideMachineSpec defines the desired state of OxideMachine.
                    properties:
                      antiAffinityGroups:
                        description: |-
                          AntiAffinityGroups is the list of anti-affinity group names or IDs that the Oxide instance
                          should use. Anti-affinity groups should be created outside the operator, else instance
                          creation will fail.
                          https://docs.oxide.computer/guides/deploying-workloads#_affinity_and_anti_affinity
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      diskSize:
                        anyOf:
                        - type: integer
                        - type: string
                        description: DiskSize is the size of the boot disk to allocate
                          for the instance, in bytes.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      imageID:
                        description: ImageID is the ID of the boot disk image.
                        type: string
                      ipPool:
                        description: |-
                          IPPool is the Oxide IP pool to use for the instance's external IP. If not set, omit the
                          external IP for the machine. If set, the IP pool must exist.
                        type: string
                      memory:
                        anyOf:
                        - type: integer
                        - type: string
                        description: Memory is the amount of memory to allocate for
                          the instance, in bytes.
                        pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                        x-kubernetes-int-or-string: true
                      nCPUs:
                        description: NCpus is the number of vCPUs to allocate for
                          the instance.
                        type: integer
                      providerID:
                        description: |-
                          ProviderID is a unique identifier for the Oxide instance for use in correlating across
                          kubernetes resources. Must match provider IDs generated by the Oxide cloud controller
                          manager.
                          https://cluster-api.sigs.k8s.io/reference/glossary#providerid
                        type: string
                      sshPublicKeys:
                        description: |-
                          SSHPublicKeys is the list of SSH key names or IDs to attach to the Oxide instance. SSH keys
                          should be created outside the operator, else instance creation will fail.
                          https://docs.oxide.computer/guides/user-settings#manage-ssh-keys
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                    required:
                    - diskSize
                    - imageID
                    - memory
                    - nCPUs
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
