apiVersion: v1
kind: Secret
metadata:
  name: capox-oxide-cloud-controller-manager
  namespace: kube-system
type: Opaque
stringData:
  oxide-host: "${OXIDE_HOST}"
  oxide-token: "${OXIDE_TOKEN}"
  oxide-project: "${OXIDE_PROJECT}"
